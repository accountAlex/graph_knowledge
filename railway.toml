[build]
builder = "NIXPACKS"
buildCommand = "npm install && cd apps/api && npx prisma generate && cd ../.. && npm run build --workspace=apps/api"

[deploy]
startCommand = "cd apps/api && npx prisma migrate deploy && node dist/main"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
